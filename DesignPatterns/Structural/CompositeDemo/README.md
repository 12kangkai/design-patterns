# Composite (CompositeDemo)

说明：
- 该项目演示设计模式：**Composite**。
- 在 `Program.cs` 中实现示例（或将实现拆分到多个源文件）。
- 目标框架： net8.0

运行示例：
```bash
dotnet run --project Structural/CompositeDemo/CompositeDemo.csproj
```

建议：
- 示例应短小（单一职责），并在输出中展示关键行为（例如创建对象／调用算法／观察者通知等）。
- 可以在同一项目中加入额外的类文件实现更完整的示例（保持文件命名清晰）。
